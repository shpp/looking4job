$( document ).ready(function() {
	// $(window).resize(function(){
	// 	var
	// 		triangleWidth = $('body').width() / 2 + 'px solid transparent';
	// $('.top-left, .bottom-left').css('border-right', triangleWidth);
	// $('.top-right, .bottom-right').css('border-left', triangleWidth);
	// });
});